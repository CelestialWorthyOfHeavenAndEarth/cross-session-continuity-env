# ── HF Space runtime deps (must all exist on PyPI) ────────────────────────────
gradio>=4.44.0
matplotlib>=3.9.0
numpy>=1.26.0
scipy>=1.13.0
pandas>=2.2.0

# openenv — install from source if not yet on PyPI:
# pip install git+https://github.com/openenv/openenv.git
