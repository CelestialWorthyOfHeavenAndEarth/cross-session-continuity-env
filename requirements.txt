# ── Space / demo (always needed) ──────────────────────────────────────────────
gradio>=4.44.0
matplotlib>=3.9.0
numpy>=1.26.0
scipy>=1.13.0
pandas>=2.2.0
pytest>=8.1.0

# ── OpenEnv ───────────────────────────────────────────────────────────────────
openenv>=0.3.0

# ── Training (Colab / HF Jobs only — heavy, skip on Space) ───────────────────
# Uncomment when running train_grpo.ipynb:
# unsloth>=2024.4.0
# trl>=0.8.6
# transformers>=4.40.0
# torch>=2.2.0
# datasets>=2.19.0
# accelerate>=0.29.0
# bitsandbytes>=0.43.0
# peft>=0.10.0
# sentencepiece>=0.2.0
# wandb>=0.17.0
